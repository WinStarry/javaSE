package com.yang.demo.demo01Collection;

/**
 * @data 2020/8/29 -16:44
 */
/*
java.lang.Object类
类 Object 是类层次结构的根类。每个类都使用 Object 作为超类。所有对象（包括数组）都实现这个类的方法。

Person类默认继承了Object类，所以可以使用Object类中的toString()方法
String toString() 返回该对象的字符串表示。

想要知道一个类是否重写了toString()方法，直接俄打印这个类对应对象的名字即可，
如果没有重写toString()方法，那么打印就是对象的地址值（默认）
如果重写了toString()方法，那么就按照重写的方式打印

 */
public class Demo01ToString {
}
